name: masamune
description: Package that supports Flutter states and their transitions using the Model Notifier package and the Katana Routing package.
version: 0.8.3+26
homepage: https://mathru.net
repository: https://github.com/mathrunet/flutter_masamune

environment:
  sdk: ">=2.13.0-0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  katana: ^0.8.0
  katana_routing: ^0.8.0
  katana_module: ^0.8.0
  model_notifier: ^0.8.0
  flutter_riverpod: ^0.13.1
  hooks_riverpod: ^0.13.1
  riverpod: ^0.13.1
  flutter_hooks: ^0.16.0
  meta: ^1.3.0
  intl: ^0.17.0
  font_awesome_flutter: ^9.0.0
  flutter_markdown: ^0.6.0
  share: ^2.0.0
  connectivity: ^3.0.2
  cached_network_image: ^3.0.0-nullsafety
  datetime_picker_formfield: ^2.0.0
  loading_animations: ^2.2.0
  responsive_grid: ^2.0.0
  cached_video_player: ^2.0.0-dev.1
  image_picker: ^0.7.3
  selectable_autolink_text: ^2.0.0
  # flutter_sequence_animation: ^3.0.1
  # bordered_text: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Masamune
  # masamune_test: ^0.5.0
  
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
