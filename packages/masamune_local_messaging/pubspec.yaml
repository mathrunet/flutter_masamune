name: masamune_local_messaging
description: Masamune package for local notification. Push notifications are sent according to pre-set local schedules.
version: 0.9.2+6
homepage: https://mathru.net
repository: https://github.com/mathrunet/flutter_masamune

environment:
  sdk: ">=2.13.0-0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

  masamune: ^0.9.0
  katana_flutter: ^0.9.0
  http: ^0.13.0
  flutter_local_notifications: ^9.1.5
  timezone: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
