name: katana_router
description: A package to automatically create routing configurations with build_runner to enable type-safe routing.
version: 1.9.7
homepage: https://mathru.net
repository: https://github.com/mathrunet/flutter_masamune/tree/master/packages/katana_router

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  katana: ^0.15.0
  katana_router_annotation: ^1.9.7
  url_strategy: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
