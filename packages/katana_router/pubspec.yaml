name: katana_router
description: A package to automatically create routing configurations with build_runner to enable type-safe routing.
version: 2.0.4
homepage: https://mathru.net
repository: https://github.com/mathrunet/flutter_masamune/tree/master/packages/katana_router

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  katana: ^2.1.0
  katana_logger: ^2.0.2
  katana_router_annotation: ^2.0.4
  url_strategy: ^0.2.0

dev_dependencies:
  test: ^1.21.3
  flutter_lints: ^2.0.0
  import_sorter: ^4.6.0
