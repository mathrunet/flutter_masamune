name: katana_module
description: Package that defines a base class for modularization. It defines abstract classes, etc. for use with the Masamune module.
version: 0.10.0+6
homepage: https://mathru.net
repository: https://github.com/mathrunet/flutter_masamune/tree/master/packages/katana_module

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  katana:
  katana_routing:
  model_notifier:
  flutter_riverpod: ^1.0.3
  riverpod: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
