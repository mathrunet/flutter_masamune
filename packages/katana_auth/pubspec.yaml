name: katana_auth
description: Base package to facilitate switching between Local and Firebase authentication implementations.
version: 1.2.12
homepage: https://mathru.net
repository: https://github.com/mathrunet/flutter_masamune/tree/master/packages/katana_auth

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  katana: ^1.0.10
  katana_logger: ^1.2.8
  path_provider: ^2.0.11
  shared_preferences: ^2.0.15

dev_dependencies:
  test: ^1.21.3
  flutter_lints: ^2.0.0
  import_sorter: ^4.6.0
