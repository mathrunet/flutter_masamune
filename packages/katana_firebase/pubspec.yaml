name: katana_firebase
description: Base package to handle firebase with Katana package. It performs initialization and other tasks.
version: 1.1.1
homepage: https://mathru.net
repository: https://github.com/mathrunet/flutter_masamune/tree/master/packages/katana_firebase

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

  katana: ^0.15.8+1
  firebase_core: ^2.1.1
  cloud_firestore: ^4.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
