name: katana_firebase
description: Katana package that provides utilities for firebase. It initializes Firebase and so on.
version: 0.13.0+5
homepage: https://mathru.net
repository: https://github.com/mathrunet/flutter_masamune/tree/master/packages/katana_firebase

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  katana_flutter: ^0.13.0
  firebase_core: ^1.10.6
  cloud_firestore: ^3.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
