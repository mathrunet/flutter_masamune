name: katana_routing
description: Package for routing. It supports page creation and page routing. And other extension methods of BuildContext.
version: 0.13.0+5
homepage: https://mathru.net
repository: https://github.com/mathrunet/flutter_masamune/tree/master/packages/katana_routing

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
    
  katana_flutter: ^0.13.0
  flutter_riverpod: ^1.0.3
  riverpod: ^1.0.3
  google_fonts: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
