name: model_notifier
description: Package that makes it easy to define ValueNotifier as a model and improves the affinity with riverpod and freezed.
version: 0.11.0+5
homepage: https://mathru.net
repository: https://github.com/mathrunet/flutter_masamune/tree/master/packages/model_notifier

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  katana: ^0.11.0
  katana_flutter: ^0.11.0
  http: ^0.13.0
  meta: ^1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
