name: katana_logger
description: Base package for logging app performance and access status. Additional packages can be installed to use Firebase Analytics, etc.
version: 1.1.1
homepage: https://mathru.net
repository: https://github.com/mathrunet/flutter_masamune/tree/master/packages/katana_logger

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  katana: ^1.0.5
  path_provider: ^2.0.11
  shared_preferences: ^2.0.15
dev_dependencies:
  test: ^1.21.3
  flutter_lints: ^2.0.0
  import_sorter: ^4.6.0
