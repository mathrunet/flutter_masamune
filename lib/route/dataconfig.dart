part of masamune;

/// Class for defining data endpoints and mockups.
class DataConfig {
  /// Endpoints.
  final String endpoint;

  /// Mockup.
  final dynamic mockup;

  /// Class for defining data endpoints and mockups.
  const DataConfig({this.endpoint, this.mockup});
}
