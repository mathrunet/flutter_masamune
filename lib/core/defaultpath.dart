part of flutter_runtime_database;

/// Define the default path.
class _DefaultPath {
  /// Routes root.
  static const String route = "route";

  /// Data loaded on the page.
  static const String pageData = "page/data";

  /// Form data on the page.
  static const String formData = "page/form";
}
